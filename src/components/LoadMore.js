const LoadMoreButton = ({ onClick }) => (
  <button
    onClick={onClick}
    className="bg-blue-500 text-white px-8 py-4 mt-4 text-lg mx-auto block"
  >
    Load More
  </button>
);

export default LoadMoreButton;
